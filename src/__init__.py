"""Core modules for the Ollama Playwright registration flow.

The original browser-based implementation lived under a hash-named snapshot
directory. The project now exposes the reusable modules as first-class imports
under ``src`` so production entrypoints can depend on stable module names.
"""
